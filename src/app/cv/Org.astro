---
import type { Org } from "./data/cv.types"
import images from "./ogimages.json"

interface Props {
  org: Org
}
const { org } = Astro.props
---

<div class="flex break-inside-avoid-page flex-row gap-2">
  <a
    class="not-prose mt-6 shrink-0"
    target="_blank"
    rel="noreferrer"
    href={org.url}
  >
    <img
      loading="eager"
      width={24}
      height={24}
      class="h-6 w-6 object-cover"
      src={org.imgURL ?? images[org.url as keyof typeof images]}
      alt={org.name}
    />
  </a>

  <div class="flex flex-col">
    <slot />
  </div>
</div>
